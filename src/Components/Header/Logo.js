
const Logo = () => {
    return <div className="flex-container logo-container">
        <img src="./amazon-smile.png" alt="logo"  onClick={() => window.location = './' }/>
    </div>
}

export default Logo